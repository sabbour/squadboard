name: Simple Review
steps:
  - type: route
    label: Route to reviewer
    agent: "{{ assignee }}"
  - type: agent_run
    label: Agent reviews the work
    agent: "{{ assignee }}"
    prompt: "Review the attached work. Check for correctness, style, and test coverage. Leave clear feedback."
    timeout: 30m
  - type: approve
    label: Approval gate
    approvers: [Lead]
    exclude_author: true
    timeout: 30m
    timeoutAction: notify
