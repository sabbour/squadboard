# Orchestration Log: Wave 25 — Keyser (Nav Collapse Regression Fix)

**Wave:** 25
**Agent:** Keyser (Frontend Dev)
**Timestamp:** 2026-05-16T02:55:00-07:00
**Status:** Complete

## Summary

Fixed collapsed-nav click handler + right-aligned expand toggle. Two bugs in the W24 collapsible navigation feature were corrected: NavItems rendered without children in collapsed mode had no DOM click targets (fixed by adding hidden span children), and the collapse toggle was center-aligned instead of right-aligned when expanded (fixed via `navCollapseToggleExpanded` style).

## Commits

- `f5d03f4f` — Collapse toggle right-align + NavItem hidden span children

## Changes

**File:** packages/client/src/components/Layout.tsx

1. NavItem click target bug:
   - Wrapped collapsed NavItems with hidden `<span className={styles.navLabelHidden}>Label</span>` to preserve DOM click target while hiding text
   - Affected: Projects, Now, all PROJECT_NAV_GROUPS (Dashboard, Board, Flow, Agents, Skills, Tools, MCP Servers, Ceremonies, Templates, Costs), Diagnostics, Heartbeat, Settings

2. Toggle alignment bug:
   - Added `navCollapseToggleExpanded` style with `justifyContent: 'flex-end'` and `paddingInlineEnd: tokens.spacingHorizontalS`
   - Applied via `mergeClasses(styles.navCollapseToggle, !navCollapsed && styles.navCollapseToggleExpanded)`
   - Result: toggle sits right-edge when expanded, centered when collapsed

## Rationale

**Option A (chosen):** Minimal diff, hidden span. CSS `display:none` guarantees text is hidden without affecting click target or layout.

**Option B (rejected):** Rely on overflow:hidden to clip text. Fragile — 56px width might not perfectly clip all label widths.

**Option C (rejected):** NavItem as="button". Not verified in NavDrawer preview docs.

## Verification

- `pnpm -r build` → green (tsc -b && vite build, 7.08s)
- No console errors
- Click targets live in collapsed mode
- Toggle aligns right when expanded

## Follow-up Rule for Future Agents

> **DO NOT render Fluent `NavItem` without children in any clickable context.** The Tooltip+NavItem-without-children pattern silently breaks click handling. Always pass children (even a hidden span) to keep the click target alive.

---

**Coordinator:** Brady
**Spawn Date:** 2026-05-16
**Wave Close Date:** 2026-05-16T02:55:00-07:00
