# Orchestration Log: Wave 25 — Verbal (Sweep Animation Viz)

**Wave:** 25
**Agent:** Verbal (Backend / Real-time / WebSocket specialist)
**Timestamp:** 2026-05-16T02:55:00-07:00
**Status:** Complete

## Summary

SweepTimeline component (60s rolling lane view) on /heartbeat full-width and /now compact 4-lane; sweep.tick WS event broadcast. Heartbeat is now visibly alive with animated pulses on each sweep completion.

## Commits

- `2fc72086` — Add SweepTimeline component + sweep.tick WS event + /heartbeat & /now integration

## Changes

**Server:**

| File | Change |
|---|---|
| `packages/server/src/realtime/event-bus.ts` | extends `HeartbeatEventType` with `'sweep.tick'` |
| `packages/server/src/engine/heartbeat.ts` | `_runSweep()` emits `sweep.tick` on success + error |
| `packages/server/src/realtime/ws-server.ts` | adds `onHeartbeat` handler, fans `sweep.tick` to `__global__` subscribers |

**Client:**

| File | Change |
|---|---|
| `packages/client/src/realtime/ws-client.ts` | adds `'sweep.tick'` to `WsEventMap` |
| `packages/client/src/components/heartbeat/SweepTimeline.tsx` | NEW — full + compact modes, 6/4 lanes, 60s sliding window, animated pulses, Fluent2-only |
| `packages/client/src/pages/Heartbeat.tsx` | adds fourth `SectionCard` with `<SweepTimeline windowSizeMs={60_000} compact={false} />` |
| `packages/client/src/pages/Now.tsx` | adds compact card after `ProjectMiniGrid` with `<SweepTimeline compact />` |

## WS Event Extension Pattern (Reference for Future)

Three coordinated edits to add new event types flowing to global subscribers:

1. **Server union:** add literal to `event-bus.ts` `HeartbeatEventType`
2. **Server fan-out:** in `ws-server.ts`, add branch forwarding to `globalClients` or `rooms` Set
3. **Client typing:** add entry to `WsEventMap` in `ws-client.ts` with payload shape

## Sweep Lane Registry

| ID | Compact? | Label |
|---|---|---|
| `ceremonies-due` | ✓ | Ceremonies |
| `ready-workflow-steps` | ✓ | Workflow Steps |
| `stuck-issue-runs` | ✓ | Stuck Runs |
| `stale-presence` | | Presence |
| `idle-live-sessions` | | Live Sessions |
| `github-sync-overdue` | ✓ | GitHub Sync |

If a new sweep is added to `index.ts`, also add to `ALL_SWEEPS` and (optionally) `COMPACT_SWEEPS` in SweepTimeline.tsx.

## Visual Design

- Each lane: `tokens.colorNeutralBackground3` track, 10px tall (7px compact)
- Pulses: circles, `tokens.colorBrandBackground` (success) or `tokens.colorPaletteRedBackground3` (error), matching halo ring, 10px (6px compact)
- Animation: CSS keyframes (`scale 0.4 → 1.25 → 1`, 0.4s)
- Window slides every 2s via `setInterval`, old pulses pruned
- Tooltip on hover: `sweepName · durationMs · status`

## Verification

- `pnpm -r build` → all 7 packages green
- Component renders with no console warnings
- Compact mode visibly shorter (22px vs 28px rows) and 4 lanes
- No emojis; only Fluent2 icons (`ArrowSync20Regular`)
- ConjureModal, Consult button, top bar, collapsed nav untouched

## Tradeoffs

- **DOM-not-canvas.** At 6 lanes × ~12 pulses/min, under 100 dots. Canvas only needed for thousands of pulses.
- **No persistence.** Refresh wipes visible window. Acceptable—Heartbeat page has historical view via `/api/heartbeat/sweeps`
- **No filter/pause.** First pass; can be added if Brady requests

## Follow-ups (Optional)

- Add "Pause" toggle to freeze window while inspecting
- Surface `agentsActivated` once coordinator-attribution lands (payload field ready)
- Integration test: boot server, fire sweep, assert `sweep.tick` reaches `__global__` WS subscriber

---

**Coordinator:** Brady
**Spawn Date:** 2026-05-16
**Wave Close Date:** 2026-05-16T02:55:00-07:00
