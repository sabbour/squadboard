# Orchestration Log: McManus r2 — Phase 3 Heartbeat Refactor

**Timestamp:** 2026-05-15T16:09:55Z
**Agent:** McManus (Lead Architect)
**Task:** Phase 3 Heartbeat refactor — 6 sweeps registered; dispatcher deprecated
**Commit:** ea091186

## Delivery

Introduced `engine/heartbeat.ts` sweep registry with 6 parallel background sweeps:

| Sweep ID | Interval | Replaces |
|---|---|---|
| `stuck-issue-runs` | 30 s | sweepExpiredLeases + sweepOrphanedRuns + etc. (5 ops) |
| `idle-live-sessions` | 60 s | New — marks `active` sessions idle after 10 min inactivity |
| `stale-presence` | 30 s | New — evicts in-memory presence records >60s old |
| `ready-workflow-steps` | 5 s | tickWorkflowAdvancement + claimAndRun (Stepper) |
| `github-sync-overdue` | 60 s | New — one-off pull for projects >5 min since lastGithubSyncAt |
| `ceremonies-due` | 5 s | sweepDueSchedules() from ceremony-scheduler |

**Minimal Sweep interface:**
```ts
interface Sweep {
  id: string;
  intervalMs: number;
  enabled: boolean;
  run(): Promise<SweepResult>;  // { acted, errors, details? }
}
```

**Dispatcher deprecation:** `dispatcher.ts` kept with comment but removed from `index.ts` import. Unused code path; rollback available.

## Event Bus

New `HeartbeatEventType` in `event-bus.ts`:
- `heartbeat.sweep.completed` — { sweepId, result, durationMs }
- `heartbeat.sweep.error` — { sweepId, error, durationMs }

Both emitted with `projectId = '__heartbeat__'` (synthetic scope).

## Status

✅ Shipped

## Follow-ups

- P2: Add `@CRON(...)` decorators or persistent schedule metadata for multi-instance deployments
