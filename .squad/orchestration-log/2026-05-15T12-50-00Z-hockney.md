# Orchestration Log: Hockney (Round 2)

**Date:** 2026-05-15T12:50:00Z  
**Agent:** Hockney (Backend Dev)  
**Task:** Image attachments backend — multer upload + bytea serve + 5MB cap  
**Mode:** Parallel (5 concurrent agents + Hockney)  
**Outcome:** ✅ Shipped

## Files produced

- `src/routes/attachments.ts` — `POST /projects/:projectId/issues/:issueId/attachments` (multer fileSize limit 5MB, MIME type whitelist: image/{png,jpeg,gif,webp,svg+xml}, inserts to `issue_attachments` bytea column, returns attachment ID + URL).
- `src/services/attachmentService.ts` — upload validation, store/retrieve from bytea, cascade delete on issue delete.
- `packages/schema/schema.ts` — added `issue_attachments` table (issueId FK, attachmentId UUID PK, content BYTEA, mimeType, created_at).

## Commit

- `2fb4291f` — feat(attachments): multer upload + bytea store + 5MB cap

## Result

Backend now accepts image uploads (5 MB max, PNG/JPEG/GIF/WebP/SVG), stores in bytea, returns immutable URLs. Frontends (McManus + Hockney) can attach images to issues without additional infra.
