# Orchestration Log — McManus W22 (2026-05-16T02:15:00Z)

**Wave:** 22  
**Agent:** McManus (Lead Architect)  
**Deliverable:** `docs/squadapp-spec.md` (1100 lines)  
**Status:** ✅ SHIPPED

## Manifest

**Objective:** Author canonical Squad Apps packaging spec; anchor Stream F (F4 curated apps, F5 import/export, F6 marketplace).

**Scope:**
- 11 sections covering format, versioning, collision handling, artifact ordering, security, examples
- JSON schema (draft-07) with validation
- 2 worked examples: `bug-repro-starter` (minimal), `aks-feature-kanban` (full)
- 10 open questions (OQ-1 through OQ-10) deferred to F5/F6
- Design decisions D1–D10 covering format, versioning, collision, rollback, secrets

**Files changed:** 1 (docs/squadapp-spec.md)

## Outcome

✅ **Spec complete and published.** Establishes:
- `squadapp.json` as distribution format (superset of existing `squad-bundle.json`)
- Idempotency contract reuses existing `bundle-loader.ts` patterns
- Fixed artifact creation order (project → kanban → skills → tools → mcp → team → routing → ceremonies → workflows → seed issues)
- Skip-with-warning collision default (matches user expectations)
- F4 ready to start immediately using `aks-feature-kanban` example as template

**Downstream clarity:** F5 can adopt squadapp as maximal bundle shape; F6 adds global registry on top; F7 uses JSON schema for CI validation.

## Lessons for McManus History

Squad Apps spec authored — foundation for F3 stream. Design for distribution format as superset of runtime format; reuse existing idempotency patterns to avoid reinventing the wheel. Defer infrastructure decisions (global registry, secrets management) to later streams; document as open questions, not blockers. Examples are executable — make them concrete enough that F4 can copy-paste.
