# Orchestration Log: redfoot (2026-05-19)

**Status:** Completed

**Spawn Manifest:**
- Removed alias claims from docs and added one-shot launch recipes for existing `.squad` repos and PostgreSQL provider mode.

**Task:** Update documentation for PostgreSQL StorageProvider no-alias follow-up

**Outcome:** Success. Documentation updated with canonical configuration and launch recipes.

**Documentation Changes:**
- Removed references to `pglite` as a DB-backed selector in README and deployment guides
- Added canonical configuration section: `SQUADBOARD_SQUAD_STORAGE_PROVIDER=postgresql` is the only DB-backed option
- Added one-shot launch recipes for:
  - Starting Squadboard with PostgreSQL provider for a new `.squad` repo
  - Migrating existing `.squad` repos to use PostgreSQL provider
  - Reverting to filesystem storage (default behavior)
- Updated changelog with no-alias decision and migration path
- Clarified safe defaults in all deployment documentation

**Validation:**
- `pnpm docs:build` passed
- `git diff --check` passed (no whitespace issues)
- All documentation builds complete without errors

**Commits:** (merged from wave session; docs build validation passed)
