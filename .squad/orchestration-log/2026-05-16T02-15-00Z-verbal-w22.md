# Orchestration Log — Verbal W22 (2026-05-16T02:15:00Z)

**Wave:** 22  
**Agent:** Verbal (Back-end Integrations)  
**Commit:** 5fdea017  
**Status:** ✅ SHIPPED

## Manifest

**Objective:** Extend the Conjure classifier from 6 → 10 intents; return top-3 candidates with confidence + reason + per-intent draft.

**Scope:**
- Extended `conjure-classifier.ts` to classify 10 intents (added `ceremony`, `mcp-server`, `inbox-item`, `consult` to the existing 6)
- New response shape: `candidates` array (top-3 by confidence)
- Request accepts `prose` (preferred) + backward-compat `prompt` alias
- New flat fields: `projectId`, `projectName`, `knownProjectNames`
- Updated `/api/conjure/classify` route to accept new contract

**Files changed:** 3 (conjure-classifier.ts, routes/conjure.ts, new test suite)

## Outcome

✅ **Classifier extended and tested.** Shipped:
- Heuristic fast-path scoring for all 10 intents
- LLM fallback returns top-3 candidates
- Per-intent draft shapes pre-built server-side
- **56 tests, all passing** (9 existing + 47 new)

**Test breakdown:** ALL_INTENTS list (2), heuristic fast-path (14), candidates array (6), LLM paths (5), per-intent drafts (9), backward compat (3), flat context (2), hint boosting (10), scoring unit (5).

**API ready:** Keyser-w22's ConjureModal automatically uses `candidates` array when present; graceful fallback if not.

## Lessons for Verbal History

Classifier extension pattern: define canonical enum of all intents, implement rule-based scorer for each, implement LLM parser that returns top-3 with confidence scores. Backward-compat field aliasing: accept both old and new names, prefer new, fail gracefully if LLM returns legacy format. Test coverage must span fast-path, LLM, and edge cases (single intent, ambiguous, degraded paths).
