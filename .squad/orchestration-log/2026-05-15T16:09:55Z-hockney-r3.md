# Orchestration Log: Hockney r3 — Anchor Filter Fix

**Timestamp:** 2026-05-15T16:09:55Z
**Agent:** Hockney (Backend / Workflow Engine Dev)
**Task:** P0 anchor filter — excludes fan-out children from resolveAnchorIssue
**Commit:** 4ecb5525

## Delivery

Fixed `resolveAnchorIssue()` in `services/ceremony-scheduler.ts` to exclude fan-out child issues (identified via `issue_links` table with `link_type = 'fan_out'`). This closes the spam loop at the source—ceremonies no longer anchor on recently-created child issues that themselves have fan-out steps.

## Scope

- Modified `resolveAnchorIssue()` query with NOT EXISTS subquery on `issue_links`
- No schema migration (discriminator already exists from Demo 10)
- Indexed by existing `(child_issue_id, link_type)` pair

## Status

✅ Shipped

## Follow-ups

- P2: Add index on `issue_links (child_issue_id, link_type)` for scale (currently O(n) scan)
- P1: Remaining sweep failure + concurrency items from Verbal's root-cause doc
