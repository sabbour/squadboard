# Orchestration Log: Kujan CI Fix

**Timestamp:** 2026-05-20T13:09:21Z
**Agent:** Kujan
**Session ID:** p0-fix-wave-may20

## Summary

Deployed CI hardening: vitest + typecheck gates now run on every PR, exposing pre-existing test and type failures.

## Commits

1. **b85520577** — CI workflow: add vitest + typecheck gates

## Changes

Updated `.github/workflows/ci.yml` to enforce:
- `pnpm --filter @sabbour/squadboard-sdk test`
- `pnpm --filter @sabbour/squadboard-client test`
- `pnpm --filter @sabbour/squadboard test -- --run`
- TypeScript noEmit checks on all packages

## Validation

- Baseline: all pre-existing failures identified
- Post-change: same red suites surfaced by CI

## Pre-existing Red Suites

1. `@sabbour/squadboard-client` — RunButton.test.tsx
2. `@sabbour/squadboard` — ceremonies-list-route, pglite-issue-run-events-catalog-repair
3. `@sabbour/squadboard` (server) — workflow-runner.ts typecheck

## Outstanding

Product team must fix existing red tests and type errors before CI gates go green.

