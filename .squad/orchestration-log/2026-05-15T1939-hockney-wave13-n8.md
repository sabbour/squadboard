# hockney-wave13-n8 — Bulk-import handler + createIssue factor

**Date:** 2026-05-15T19:39:32-07:00  
**Wave:** 13  
**Task:** N8 (Wave 13 / Ahmed design call, pre-Wave-13 work)  
**Model:** claude-sonnet-4.6  

## Deliverable

**One-Handler-Three-Adapters refactor + bulk-import service:**

1. Unified `createIssue()` handler in `services/issues.ts`
   - Idempotency key + 60-second soft dedup
   - Auto-set `completedAt` when status='done'
   - Column validation in HTTP layer only
2. Adapters:
   - MCP: `handleCreateIssue` delegates to service
   - HTTP: POST `/api/projects/:id/issues` routes validation → service
   - CLI: `bin/squad-bulk-import` + `src/cli/bulk-import.ts`
3. Bulk-import service: `bulkImportIssues(input)`
   - Per-row try/catch (failures don't abort batch)
   - **Inertness invariant:** only `status ∈ {backlog, done}` allowed
   - Dry-run mode (SELECT only)

## Bulk Port Results (2026-05-15)

- Input: 131 entries from `/tmp/todos.json` (48 done, 83 pending)
- Pre-import: 35 issues in project `foo`
- **Dry run:** 131 would-create, 0 would-skip, 0 errors ✓
- **Real run:** created=131, skipped=0, errors=0 ✓
- Post-import: 166 issues (35 + 131)
- Spot-checks: inert status, no assignee, no labels — all ✓

## Status

✓ N8 complete; tests 4/4 passing; committed

## Files Changed

- packages/server/src/services/issues.ts (new `createIssue()` signature)
- packages/server/src/services/bulk-import-issues.ts (new)
- packages/server/src/cli/bulk-import.ts (new)
- packages/server/src/mcp/server.ts (delegation)
- packages/server/src/routes/issues.ts (delegation)
- packages/server/src/db/schema.ts (added `completed_at`, `created_by`)
- packages/server/src/db/index.ts (migration)
- bin/squad-bulk-import (new shell shim)
- packages/server/__tests__/issues-service.test.ts (new smoke tests)

## Source Commit

- **SHA:** 492a035c
- **Message:** `feat(server): factor createIssue, add bulk-import CLI + 131-card port`

## Deferred

Wave-13 N9: MCP wrapper `squadboard_bulk_import_cards` tool (handler ready, N9 just adds tool wiring)

---

*Orchestrated by: Scribe*  
*Session: Wave 13 Scribe Close-out*
