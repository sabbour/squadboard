# Orchestration Log — Keyser W26

**Wave:** 26  
**Agent:** Keyser  
**Spawn:** 2026-05-16T10:33:30Z  
**Commit:** 53cba6eb  

## Plain-Language Summary

Four UI bugs in one batch: (1) RoutingLogTable crashes when server returns non-array or type-mismatched score, fixed with runtime type checks + ErrorBoundary; (2) Reassign and Run actions incorrectly open detail panel due to event bubbling, fixed by moving footer outside `onClick` wrapper and adding reassign agent-picker dropdown; (3) Dead "Investigate in Conjure" MenuItem removed from CardDetail header; (4) Heartbeat nav link breaks project scope by navigating to `/heartbeat` instead of `/projects/:id/heartbeat`, fixed to preserve scope context. All four fixes ship together in commit 53cba6eb. No regression tests added (pre-existing test infrastructure in packages/client is sparse).

## Lineage

- **Spawned:** Brady (W26 batch 1)
- **Input:** 4 UI bugs from deployment feedback
- **Deliverable:** Commit 53cba6eb; 4 bug fixes; 1 new API hook (useAssignIssue)
- **Closed:** 4 todos (w26-routing-crash, w26-reassign-run-panel, w26-dead-conjure, w26-heartbeat-scope)

## Files Changed

- `packages/client/src/components/board/IssueCard.tsx`
- `packages/client/src/components/board/CardDetail.tsx`
- `packages/client/src/components/routing/RoutingLogTable.tsx`
- `packages/client/src/components/routing/Agents.tsx`
- `packages/client/src/layout/Layout.tsx`
- `packages/client/src/api/issues.ts`

## Post-Batch-1 Regression (W26)

Keyser's stopPropagation fix (Bug 2) incidentally removed the visual feedback that previously came from the panel opening. User testing revealed: clicking Run now dispatches correctly BUT appears to do nothing because the button's `isPending` style didn't change. Fixed in follow-up commit 57429237 with button text + opacity feedback and transition animation.

## Observations

UI refactors without component-test coverage expose visual feedback regressions in prod. Keyser's self-fix (commit 57429237) demonstrates the regression was visual-only; the backend dispatch was working. All four bugs were frontend-surface only; server/engine untouched.
