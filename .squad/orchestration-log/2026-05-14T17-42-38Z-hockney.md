# Orchestration Log: hockney (2026-05-14)

**Status:** Completed

**Spawn Manifest:**
- Added GitHub App schema columns (github_auth_type, github_app_id, github_app_installation_id, github_app_private_key) to projects table + bootstrapSchema migrations. Committed to main.

**Task:** Extend projects schema for GitHub App authentication

**Outcome:** Success. Added four new columns to support GitHub App auth alongside existing PAT auth.

**Schema Additions:**
| Column | SQL name | Type | Notes |
|---|---|---|---|
| `githubAuthType` | `github_auth_type` | TEXT | `'pat'` or `'app'`; NULL treated as `'pat'` for backward compat |
| `githubAppId` | `github_app_id` | TEXT | Numeric GitHub App ID stored as string |
| `githubAppInstallationId` | `github_app_installation_id` | TEXT | Installation ID scoped to org/repo |
| `githubAppPrivateKey` | `github_app_private_key` | TEXT | PEM private key (plaintext in hacking phase) |

**Decisions:**
- Nullable TEXT for authType (avoids DB enum ceremony, enforced at app layer)
- Plaintext PEM acceptable for local dev; production requires secrets manager
- Backward compatible: existing rows treated as PAT auth

**Implementation Notes:**
- `bootstrapSchema()` uses `ALTER TABLE … ADD COLUMN IF NOT EXISTS` for safety
- API/client logic is a follow-up task
- Private key must never be logged or returned in list-projects responses

**Commit:** Merged to main
