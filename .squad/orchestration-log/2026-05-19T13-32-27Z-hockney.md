# Orchestration Log: hockney (2026-05-19)

**Status:** Completed

**Spawn Manifest:**
- Removed the `pglite` compatibility alias from runtime selection. `resolveStorageBackend()` now selects PostgreSQL only for `SQUADBOARD_SQUAD_STORAGE_PROVIDER=postgresql`; unset, `fs`, `pglite`, and unknown values stay filesystem-backed.

**Task:** Implement no-alias follow-up for PostgreSQL StorageProvider

**Outcome:** Success. Canonical `postgresql` is now the only DB-backed selector.

**Implementation Details:**
- `resolveStorageBackend()` refactored to treat `pglite` as default-safe (filesystem fallback)
- Only explicit `SQUADBOARD_SQUAD_STORAGE_PROVIDER=postgresql` triggers DB-backed storage
- Environment variable detection logic hardened; stale or unknown values default to safe filesystem mode
- Backward compatibility: existing `.squad` repos can be migrated by setting the env var at launch time

**Decisions:**
- Remove silent compatibility aliases; explicit opt-in only
- Preserve filesystem as the default for safety
- Document launch recipes for new and existing `.squad` repos

**Validation:**
- Provider-focused tests: 72/72 passed
- Full server build passed
- Whitespace cleanup verified with `git diff --check`

**Commits:** (merged from wave session; provider suite validation passed)
