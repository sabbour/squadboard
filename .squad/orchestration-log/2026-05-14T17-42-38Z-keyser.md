# Orchestration Log: keyser (2026-05-14)

**Status:** Completed

**Spawn Manifest:**
- Fixed root dev script — pnpm now runs both server+client in parallel. Committed to main.

**Task:** Update dev script to run client and server concurrently

**Outcome:** Success. Updated `package.json` `dev` script from single server command to parallel server+client execution using pnpm native multi-filter support. Changed from:
```
"dev": "pnpm --filter @sabbour/squadboard-server dev"
```
to:
```
"dev": "pnpm --filter @sabbour/squadboard-server --filter @sabbour/squadboard-client run dev"
```

**Decision:**
- Uses pnpm's native multi-filter support (cross-platform, no additional dependencies)
- Express server runs on :3000, Vite on :5173
- Vite proxy already configured for `/api` → `:3000`

**Commit:** Merged to main
