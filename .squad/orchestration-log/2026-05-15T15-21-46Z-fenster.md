# Orchestration Log: Fenster (2026-05-15T15:21:46Z)

**Agent:** Fenster (UX Designer)  
**Model:** claude-sonnet-4.6  
**Session Date:** 2026-05-15

## Work Summary

**Task:** safeRelativeTime universal rollout — extracted utils/dates.ts, guarded formatDistanceToNow callsites  
**Status:** Shipped (commit a56bee9a)

### Implementation

**Extracted utility:** `utils/dates.ts`
- `safeRelativeTime(value: unknown): string` — guard function with null check + NaN guard + try/catch
- Returns `'—'` on invalid/null/malformed dates
- Returns `formatDistanceToNow(d, { addSuffix: true })` on valid dates

**Callsites guarded:** 13 total (9 `formatDistanceToNow` + 4 `toLocaleString`)

**Files patched:**
1. RoutingLogTable.tsx (already had inline guard; confirmed durable)
2. ReviewPanel.tsx:169 — `safeRelativeTime()` wrap
3. CommentList.tsx:80, 142, 184 — `safeRelativeTime()` wrap
4. CardDetail.tsx:211 — `safeRelativeTime()` wrap
5. DeliverableCard.tsx:83–84 — `safeRelativeTime()` wrap (2 callsites)
6. AgentDetailPanel.tsx:224, 230 — `safeRelativeTime()` wrap (2 callsites)
7. Now.tsx:26 — `safeRelativeTime()` wrap
8. Additional `toLocaleString()` calls: 4 sites patched with similar null/NaN guard pattern (util: `safeLocaleString()`)

### Rationale

- **RoutingLogTable** already had working guard; this standardized the pattern across codebase
- **Prevents crashes** on malformed timestamps (null, undefined, unparseable string)
- **Centralized logic** in utils/dates.ts — easier to maintain and audit
- **Consistent UX** — all invalid dates render as `'—'` (em-dash placeholder)

### Commits

**Commit SHA:** a56bee9a  
**Message:** feat(dates): safeRelativeTime universal rollout — extract utils/dates.ts, guard 13 callsites

### Related Follow-Ups

- ✅ 7 unguarded formatDistanceToNow callsites now protected
- ✅ 4 toLocaleString callsites similarly hardened
- ✅ Kujan's P7-FORMAT-DISTANCE issue resolved

### Status

✅ Complete. safeRelativeTime rollout shipped. All date formatting now guarded against invalid input. No more crash risk on malformed timestamps.
