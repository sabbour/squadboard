# Orchestration Log — Hockney W21 (2026-05-16T02:15:00Z)

**Wave:** 21 (closed in Wave 22's accounting)  
**Agent:** Hockney (Platform / Storage / Reliability)  
**Commit:** 64280223  
**Status:** ✅ SHIPPED + CLOSED

## Manifest

**Objective:** Fix the PGlite NodeFS persistence bug blocking migration replay; add graceful shutdown + stale-run recovery.

**Scope:**
1. **Migration Replay Fix:** Replace NodeFS persistence with dumpDataDir → loadDataDir restore cycle. CHECKPOINT before exit; call stopPglite() cleanly; restore via PGlite.create({ loadDataDir }).
2. **Graceful Shutdown:** Enhanced gracefulShutdown() to drain 10s, issue CHECKPOINT, close DB; added drain timeout ref.
3. **Stale-Run Recovery:** Boot-time UPDATE marks any `status='running'` rows as failed with `stale_reason='restart-pickup'`.
4. **Bug Fixes:** TDZ hoisting of `seedGhCardSideEffects` in db/index.ts; daemon `auto-start.ts` spawn error handler.

**Files changed:** 8 (migration, restore, db schema/index, graceful-shutdown test, github-actions e2e test, daemon)

## Outcome

✅ **Migration replayed. 225 issues restored (was 0).**

| Table | Before | After |
|-------|--------|-------|
| issues | 0 | 225 |
| projects | 0 | 3 |
| step_runs | 0 | 72 |
| handoff_context | 0 | 54 |
| **Total rows** | ~21 | ~656 |

**Test coverage:** 5 new graceful-shutdown tests; all 14 server tests pass.

**Lesson learned:** PGlite NodeFS is unreliable for complex schemas. dumpDataDir → runRestore cycle is the only reliable persistence method for WASM restarts. CHECKPOINT must be issued before process exit; stale-run recovery on boot prevents hung processes from blocking users.

## Lessons for Hockney History

PGlite NodeFS persistence is unreliable for complex schemas — use dumpDataDir → runRestore cycle instead. CHECKPOINT before exit always. Stale-run recovery pattern: mark orphaned running rows as failed with specific stale_reason on boot. Graceful shutdown must drain inflight requests (10s timeout), flush WAL, close DB cleanly. Daemon spawn errors must have .on('error') handlers; use full paths instead of bare binary names.
