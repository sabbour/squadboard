# Orchestration Log — Hockney W26

**Wave:** 26  
**Agent:** Hockney  
**Spawn:** 2026-05-16T10:33:30Z  
**Commit:** c762610a  

## Plain-Language Summary

Charter parser was forwarding markdown bold syntax (`**Preferred:** auto`) to the platform API instead of extracting the sentinel value `auto`. The parser's `case 'model':` handler only stripped the leading bullet, leaving `**Preferred:** auto` intact. Root cause: missing markdown syntax unwrap in the regex. Fix: added pattern detection for `**Key:** value` (bold) and `Key: value` (plain text) with extraction of just the value portion. Also guards sentinel values `auto`, `default`, and empty strings — these now result in `undefined` model field, allowing the platform to select. Verified with 12 new regression tests in `charter-parser.test.ts`. All existing tests pass.

## Lineage

- **Spawned:** Brady → Coordinator (W26 queue)
- **Input:** "Runs fail with Model '**Preferred:** auto' is not available"
- **Deliverable:** Commit c762610a; 12 test cases; charter-compiler.ts fix
- **Closed:** 1 todo (`w26-charter-parser`)

## Files Changed

- `packages/server/src/services/charter-compiler.ts`
- `packages/server/src/__tests__/charter-parser.test.ts`

## Test Results

```
12/12 vitest — PASS
pnpm -r build — PASS
```

## Observations

Model sentinel detection is now robust to markdown formatting variations. Prevents future API contract violations where malformed model strings reach the platform. No downstream impact — server-side only.
