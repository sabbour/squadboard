# Wave 23 Health Report

**Date:** 2026-05-16T02:55:00Z  
**Session:** 4fa34ed1-d2fd-4363-8668-f63188a1cfe3  
**Coordinator:** Scribe  

---

## (a) Wave Summary

**3 spawns launched, 3 completed, 0 build breaks**

- **F4 (Kobayashi)** — AKS Feature Kanban: First curated Squad App
  - 20 files (squadapp.json, project.json, 4 agents, 3 ceremonies, 2 skills, 1 tool, 1 MCP recipe, 5 seed issues, README)
  - 3 verbatim spec quotes documented
  - 5 spec gaps flagged for McManus W24

- **I7 (Hockney)** — Idempotency on MCP capture + writes
  - Project-scoped partial unique indexes on `inbox_items` + `issues`
  - Auto-generated sha256 keys (sha256(projectId + '\0' + normalizedPrompt)[0:32])
  - 5 Vitest tests (12 assertions) — all pass
  - ⚠️ Live tsx server needs manual restart (WSL inotify miss)

- **UI (Keyser)** — Full Fluent Icon Sweep
  - 106 unicode emoji glyphs replaced across 41 files
  - Build clean (3561 modules, 0 TS errors)
  - 40-entry emoji-to-Fluent mapping table established
  - Structural changes: Pill API, KIND_ICON functions, SVG text fallback

**Status:** All spawns COMPLETE. No blockers. Tests passing.

---

## (b) Backlog Delta

**Before Wave 23:**
- Pending: 37 issues/todos
- Done: 165 completed

**After Wave 23 (estimated):**
- Pending: ~34 issues/todos (3 closed: F4, I7, UI)
- Done: ~168 completed

**Movement:** F4, I7, UI closed. Backlog reduced by 3 pending. No new blocker issues opened.

---

## (c) Lineage Tree

```
Wave 23 Spawn Manifest
├── F4 — AKS Feature Kanban (Kobayashi)
│   ├── Todo: f4-aks-feature-kanban-first-app ✓ CLOSED
│   ├── Deliverable: bundles/aks-feature-kanban/ (20 files)
│   └── Schema: packages/server/src/services/squad-apps/schema.json
│
├── I7 — Idempotency on MCP Writes (Hockney)
│   ├── Todo: i7-idempotency-mcp-writes-impl ✓ CLOSED
│   ├── Deliverable: project-scoped dedup keys, 5 tests
│   └── Impact: inbox_items + issues schema changes
│
└── UI — Fluent Icon Sweep (Keyser)
    ├── Todo: ui-emoji-sweep-w23 ✓ CLOSED
    ├── Deliverable: 41 files updated, 106 glyphs → Fluent
    └── Companion: ui-no-emoji-fluent-icons-policy ✓ CLOSED
```

---

## (d) Defects Observed

1. **Server restart required (Hockney)** — WSL inotify did not trigger auto-reload after I7 migration. Coordinator will flag to user: manual `npm run dev` restart needed.

2. **5 Spec Gaps Flagged for McManus W24 (Kobayashi)**
   - **SF-1:** `kind` enum field (discriminator for marketplace: `project-template | skill-pack | ceremony-pack`)
   - **SF-2:** `displayName` field (separate from `name` for UI display)
   - **SF-3:** `artifacts` manifest section (document all included files)
   - **SF-4:** `agents/` as alternative directory discovery path (vs. `team/`)
   - **SF-5:** Ajv `strict: false` needed for `format: uri` validation

---

## (e) Verbatim Quotes

### Kobayashi-w23 (F4 — AKS Feature Kanban)

> "First Squad App shipped. Pattern: spec-quotes-in-decision-doc enforced; flag spec gaps as W24 follow-up todos."

### Hockney-w23 (I7 — Idempotency)

> "Idempotency pattern: sha256(scope-id + canonical-payload)[0:32]; partial unique indexes scoped per project; backward-compat null-key legacy path preserved."

### Keyser-w23 (UI — Fluent Icon Sweep)

> "Fluent icon sweep at scale: 41 files, 106 violations in one pass via systematic mapping table. Keep the mapping table for re-use."

---

## (f) Next-Wave Recommendations

### Immediate (W24)

1. **McManus W24 — Squad Apps Spec Refinements**
   - Address SF-1 through SF-5 (from Kobayashi's flagged gaps)
   - Finalize `kind` enum, `displayName`, `artifacts`, `agents/` discovery
   - Document Ajv format validator dependency

2. **Server Restart Guide**
   - Document in operator runbooks: after I7 idempotency migration, tsx server requires manual restart on WSL (auto-reload unreliable)

### Near-term (W24–W26)

3. **F5 — Migrate Existing Bundles to New Format**
   - 5 community/internal Squad Apps → new squadapp.json format
   - Use F4 AKS Feature Kanban as template
   - Apply spec refinements from McManus W24 as they land

4. **SDK HTTP Client**
   - `squadboard-sdk` currently has no HTTP write surface
   - When available, expose `idempotencyKey?: string` on each write method
   - Pair with `report_bug` / `add_feature` / `add_chore` MCP tools

5. **L2 Electron Scaffold**
   - Stream L (infrastructure) — Electron app for local desktop deployments
   - Gateway for non-npm users (Docker, standalone binary, installer)

### Future (W27+)

6. **UI — Remaining Spinner Sweep**
   - 8+ sites with tiny spinners documented in Keyser history (ProjectPicker, SystemBackup, etc.)
   - Deferred to next wave (surgical scope, focused PRs)

7. **Marketplace Discovery**
   - Once `kind` enum stabilizes (W24), enable Squad App marketplace discovery
   - `kind` field used as discriminator for filtering

---

## Closes

- f4-aks-feature-kanban-first-app
- i7-idempotency-mcp-writes-impl
- ui-emoji-sweep-w23
- ui-no-emoji-fluent-icons-policy

## Coordinator Action Items

- ⚠️ **User notification:** I7 migration deployed; manual tsx server restart required (WSL inotify miss)
- **Backlog cleanup:** Mark 3 closed todos as done in squad.agent.md
- **W24 kickoff:** Route Kobayashi's 5 spec gaps to McManus for Q4 spec refinement cycle
