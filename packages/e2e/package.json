{
  "name": "@sabbour/squadboard-e2e",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "demo:record": "lsof -ti tcp:5173 tcp:3000 2>/dev/null | xargs -r kill -9 2>/dev/null; sleep 2; rm -rf .demo-home; playwright test --config=playwright.demo.config.ts --reporter=line"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
