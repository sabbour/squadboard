{
  "name": "@sabbour/squadboard-coordinator-fragment",
  "version": "0.1.0",
  "description": "DEPRECATED — coordinator fragment and postinstall are now part of @sabbour/squadboard (packages/server). This package is private.",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "node scripts/postinstall-coordinator-fragment.mjs"
  }
}
